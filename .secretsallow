# Reviewed credential-scan baseline.
#
# Each line is a fingerprint (path + truncated salted hash), never a value.
# Adding a line means a human decided the finding is not an unrotated live
# credential. Every entry needs a comment saying why, and a way out.
#
# Format: <path>:<hash>

# ---------------------------------------------------------------------------
# KNOWN EXPOSURE — NOT ACCEPTED, ONLY RECORDED. See issue #12.
#
# These are real credentials for the Meri Panchayat / Panchayat Nirnay portal
# APIs, merged to dev in b5c2edc and live in a public repository. They are
# baselined solely so this scanner can be turned on now and block *new*
# leaks instead of waiting for the cleanup. They must be revoked and rotated,
# after which these two lines get deleted and the scan stays green on its own.
#
# Do not add anything else to this section.
# ---------------------------------------------------------------------------
src/ingest/Panchayat_Nirnay/config.py:b69d5840f00082a0  # portal access key, pending rotation
src/ingest/Panchayat_Nirnay/config.py:27a86a85d36fbf3b  # portal secret key, pending rotation
