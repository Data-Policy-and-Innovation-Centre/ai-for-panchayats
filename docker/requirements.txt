# The consumer application's own pins.
fastapi>=0.115
uvicorn>=0.30
pandas>=2.0
duckdb>=1.5.5
python-dotenv>=1.0
openai>=2.0
rapidfuzz>=3.0
numpy>=1.24
psycopg2-binary>=2.9
# Snapshot retrieval. pyngrok is deliberately omitted: tunnelling out of a
# task in a private-ish subnet is not how this is reached.
boto3>=1.40.0
